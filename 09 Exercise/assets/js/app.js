"use strict";
/*
let subject = [];

let j = 1;

const namearr = [];

function createJson() {
  for (let i = 1; i < 100; i++) {
    let obj;

    const totalrandom = Math.round(Math.random() * (100000 - 10000) + 10000);
    const uniquerandom = Math.round(Math.random() * (10000 - 1000) + 1000);

    obj = {
      id: i,
      total_views: totalrandom,
      unique_views: uniquerandom,
    };

    subject.push(obj);

    JSON.stringify(subject);

    console.log(subject);

    // console.log(txt, typeof txt);

    localStorage.setItem("data", JSON.stringify(subject));
  }


}

createJson();


*/
